# Server Port
server.port=8083

# Spring Application Name
spring.application.name=mail-service

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nguyendinhthongdhsg@gmail.com
spring.mail.password=auxhnuproxxopoug
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Kafka Consumer
spring.kafka.consumer.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=mail_group
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=*

spring.kafka.consumer.properties.spring.json.type.mapping=org.example.bookvexebej2e.models.dto.kafka.MailKafkaDTO:com.bookvexe.mailservice.dto.MailKafkaDTO

# Spring Batch Configuration
spring.batch.jdbc.initialize-schema=always
spring.batch.job.enabled=false
mail.dry-run=true

# Database Connection
spring.datasource.url=jdbc:postgresql://localhost:5441/bookvexedb_j2e
spring.datasource.username=root
spring.datasource.password=nopass

# Enable SQL logging
spring.jpa.show-sql=true

# Hibernate DDL auto setting (was originally 'update', now explicitly 'none')
spring.jpa.hibernate.ddl-auto=none

# Format SQL output
spring.jpa.properties.hibernate.format_sql=true

# Set Hibernate dialect for PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Log SQL binding parameters to get actual query values
spring.jpa.properties.hibernate.use_sql_comments=true

# Previously used: spring.jpa.hibernate.ddl-auto=update
# spring.jpa.hibernate.ddl-auto=update

# Exclude auto-configuration for JDBC DataSource
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration